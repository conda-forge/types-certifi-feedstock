{% set name = "types-certifi" %}
{% set version = "2021.10.8.2" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/types-certifi-{{ version }}.tar.gz
  sha256: a36dc1cb4aeeea8c97430297db10b4c0481ba612ed443e48e3e8aa091f359440

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - python >=3.6

test:
  commands:
    - test -f $SP_DIR/certifi-stubs/__init__.pyi


about:
  home: https://github.com/python/typeshed
  summary: Typing stubs for certifi
  license: Apache-2.0 AND MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - fhoehle
    - conda-forge/mypy
